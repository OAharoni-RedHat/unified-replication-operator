# Invalid VolumeReplication - Missing required fields
# This demonstrates validation errors for v1alpha2 API
apiVersion: replication.unified.io/v1alpha2
kind: VolumeReplication
metadata:
  name: invalid-extensions
  namespace: default
spec:
  # Missing required field: volumeReplicationClass
  # Missing required field: pvcName
  # Missing required field: replicationState
  # This will fail validation
