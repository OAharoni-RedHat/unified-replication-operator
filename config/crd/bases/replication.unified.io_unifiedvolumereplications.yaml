---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: unifiedvolumereplications.replication.unified.io
spec:
  group: replication.unified.io
  names:
    kind: UnifiedVolumeReplication
    listKind: UnifiedVolumeReplicationList
    plural: unifiedvolumereplications
    shortNames:
    - uvr
    - unifiedvr
    singular: unifiedvolumereplication
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.replicationState
      name: State
      type: string
    - jsonPath: .spec.replicationMode
      name: Mode
      type: string
    - jsonPath: .spec.volumeMapping.source.pvcName
      name: Source
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: UnifiedVolumeReplication is the Schema for the unifiedvolumereplications
          API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: UnifiedVolumeReplicationSpec defines the desired state of
              UnifiedVolumeReplication
            properties:
              destinationEndpoint:
                description: DestinationEndpoint defines the destination replication
                  endpoint
                properties:
                  cluster:
                    description: Cluster identifier for the Kubernetes cluster
                    minLength: 1
                    type: string
                  region:
                    description: Region identifier for geographic location
                    minLength: 1
                    type: string
                  storageClass:
                    description: StorageClass name for the storage system
                    minLength: 1
                    type: string
                required:
                - cluster
                - region
                - storageClass
                type: object
              extensions:
                description: Extensions for vendor-specific configurations
                properties:
                  ceph:
                    description: Ceph-specific extensions
                    properties:
                      mirroringMode:
                        description: MirroringMode specifies the RBD mirroring mode
                        enum:
                        - journal
                        - snapshot
                        type: string
                      schedulingStartTime:
                        description: SchedulingStartTime specifies when scheduled
                          replication should start
                        format: date-time
                        type: string
                    type: object
                  powerstore:
                    description: PowerStore-specific extensions
                    properties:
                      rpoSettings:
                        description: RPO settings for PowerStore replication
                        enum:
                        - Five_Minutes
                        - Fifteen_Minutes
                        - Thirty_Minutes
                        - One_Hour
                        type: string
                      volumeGroups:
                        description: VolumeGroups for consistency group management
                        items:
                          type: string
                        type: array
                    type: object
                  trident:
                    description: Trident-specific extensions
                    properties:
                      actions:
                        description: Actions to perform on the Trident replication
                        items:
                          description: TridentAction defines an action to be performed
                            on Trident replication
                          properties:
                            snapshotHandle:
                              description: SnapshotHandle for the action
                              type: string
                            type:
                              description: Type of action to perform
                              enum:
                              - mirror-update
                              type: string
                          required:
                          - type
                          type: object
                        type: array
                    type: object
                type: object
              replicationMode:
                description: ReplicationMode defines the replication consistency mode
                enum:
                - synchronous
                - asynchronous
                - eventual
                type: string
              replicationState:
                description: ReplicationState defines the desired replication state
                enum:
                - source
                - replica
                - promoting
                - demoting
                - syncing
                - failed
                type: string
              schedule:
                description: Schedule defines the replication scheduling configuration
                properties:
                  mode:
                    description: Mode defines the scheduling approach
                    enum:
                    - continuous
                    - interval
                    - manual
                    type: string
                  rpo:
                    description: RPO (Recovery Point Objective) - maximum acceptable
                      data loss duration
                    pattern: ^[0-9]+(s|m|h|d)$
                    type: string
                  rto:
                    description: RTO (Recovery Time Objective) - maximum acceptable
                      recovery time
                    pattern: ^[0-9]+(s|m|h|d)$
                    type: string
                required:
                - mode
                type: object
              sourceEndpoint:
                description: SourceEndpoint defines the source replication endpoint
                properties:
                  cluster:
                    description: Cluster identifier for the Kubernetes cluster
                    minLength: 1
                    type: string
                  region:
                    description: Region identifier for geographic location
                    minLength: 1
                    type: string
                  storageClass:
                    description: StorageClass name for the storage system
                    minLength: 1
                    type: string
                required:
                - cluster
                - region
                - storageClass
                type: object
              volumeMapping:
                description: VolumeMapping defines the source to destination volume
                  mapping
                properties:
                  destination:
                    description: Destination volume information
                    properties:
                      namespace:
                        description: Namespace for the destination volume
                        minLength: 1
                        type: string
                      volumeHandle:
                        description: VolumeHandle is the backend-specific volume identifier
                        minLength: 1
                        type: string
                    required:
                    - namespace
                    - volumeHandle
                    type: object
                  source:
                    description: Source volume information
                    properties:
                      namespace:
                        description: Namespace containing the PVC
                        minLength: 1
                        type: string
                      pvcName:
                        description: PVC name in the source cluster
                        minLength: 1
                        type: string
                    required:
                    - namespace
                    - pvcName
                    type: object
                required:
                - destination
                - source
                type: object
            required:
            - destinationEndpoint
            - replicationMode
            - replicationState
            - schedule
            - sourceEndpoint
            - volumeMapping
            type: object
          status:
            description: UnifiedVolumeReplicationStatus defines the observed state
              of UnifiedVolumeReplication
            properties:
              conditions:
                description: Conditions represent the latest available observations
                  of the replication's current state
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              discoveredBackends:
                description: DiscoveredBackends lists the storage backends discovered
                  in the cluster
                items:
                  description: BackendInfo provides information about discovered storage
                    backends
                  properties:
                    available:
                      description: Available indicates if the backend is available
                      type: boolean
                    capabilities:
                      description: Capabilities lists the backend's capabilities
                      items:
                        type: string
                      type: array
                    name:
                      description: Name of the backend
                      type: string
                    type:
                      description: Type of the backend
                      enum:
                      - ceph-csi
                      - trident
                      - powerstore
                      type: string
                  required:
                  - available
                  - name
                  - type
                  type: object
                type: array
              observedGeneration:
                description: ObservedGeneration reflects the generation of the most
                  recently observed spec
                format: int64
                type: integer
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
