---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.5
  name: volumereplications.replication.unified.io
spec:
  group: replication.unified.io
  names:
    kind: VolumeReplication
    listKind: VolumeReplicationList
    plural: volumereplications
    shortNames:
    - vr
    - volrep
    singular: volumereplication
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.replicationState
      name: State
      type: string
    - jsonPath: .spec.pvcName
      name: PVC
      type: string
    - jsonPath: .spec.volumeReplicationClass
      name: Class
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha2
    schema:
      openAPIV3Schema:
        description: |-
          VolumeReplication is the Schema for the volumereplications API
          This resource is compatible with kubernetes-csi-addons VolumeReplication
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: VolumeReplicationSpec defines the desired state of VolumeReplication
            properties:
              autoResync:
                description: AutoResync indicates if the volume should be automatically
                  resynced
                type: boolean
              dataSource:
                description: DataSource is an optional data source for cloning scenarios
                properties:
                  apiGroup:
                    description: |-
                      APIGroup is the group for the resource being referenced.
                      If APIGroup is not specified, the specified Kind must be in the core API group.
                      For any other third-party types, APIGroup is required.
                    type: string
                  kind:
                    description: Kind is the type of resource being referenced
                    type: string
                  name:
                    description: Name is the name of resource being referenced
                    type: string
                required:
                - kind
                - name
                type: object
                x-kubernetes-map-type: atomic
              pvcName:
                description: PvcName is the name of the PersistentVolumeClaim to be
                  replicated
                minLength: 1
                type: string
              replicationState:
                description: ReplicationState represents the desired replication state
                enum:
                - primary
                - secondary
                - resync
                type: string
              volumeReplicationClass:
                description: VolumeReplicationClass is the name of the VolumeReplicationClass
                minLength: 1
                type: string
            required:
            - pvcName
            - replicationState
            - volumeReplicationClass
            type: object
          status:
            description: VolumeReplicationStatus defines the observed state of VolumeReplication
            properties:
              conditions:
                description: Conditions represent the latest available observations
                  of the replication's current state
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              lastSyncDuration:
                description: LastSyncDuration represents the duration of last sync
                  operation
                type: string
              lastSyncTime:
                description: LastSyncTime represents the time of last successful synchronization
                format: date-time
                type: string
              message:
                description: Message provides detailed information about the current
                  state
                type: string
              observedGeneration:
                description: ObservedGeneration reflects the generation of the most
                  recently observed spec
                format: int64
                type: integer
              state:
                description: State represents the current replication state
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
